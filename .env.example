# Optional local env file. Copy to `.env` if you use a dotenv loader.
# Do NOT commit real tokens. Prefer:
#   huggingface-cli login
# or a session env var:
#   export HF_TOKEN=YOUR_TOKEN_HERE          # bash
#   $env:HF_TOKEN = "YOUR_TOKEN_HERE"      # PowerShell

HF_TOKEN=
